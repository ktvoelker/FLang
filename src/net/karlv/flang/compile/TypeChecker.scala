package net.karlv.flang.compile
import net.karlv.flang.ast._
import scalaz._
import Scalaz._

object TypeChecker extends Function1[UMod.Expr, UMod.Expr] {
  
  def apply(expr: UMod.Expr): UMod.Expr = {
    null
  };

}