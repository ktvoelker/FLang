
TO-DO
=====

Write scope-linking code
  This is easier to do as expr nodes are constructed, rather than as a separate phase

Improve error messages in the parser

Add syntax for decision tables

Add a new form of type constraints that limit effects

Use packrat parsers
